<SearchBoxField>:
    canvas.before:
        Color:
            rgba: field_highlight_color if self.mouse_over else (0, 0, 0, 0)

        Rectangle:
            pos: 0, 0
            size: self.size

    Label:
        text: "Testing"
        color: text_color_when_highlighted if root.mouse_over else text_color
        text_size: self.size